# Server Configuration
server.port=8080

# Database Configuration
spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:papertrading}
spring.datasource.username=${DB_USER:postgres}
spring.datasource.password=${DB_PASSWORD:postgres}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

# JWT Configuration
jwt.secret=${JWT_SECRET:your-super-secret-key-that-is-at-least-32-bytes-long-for-production}
jwt.expiration=${JWT_EXPIRATION:86400000}

# Alpha Vantage Configuration (get key from https://www.alphavantage.co/api/)
alpha.vantage.api.key=OAX5JCSLSEDLZR19
alpha.vantage.api.url=https://www.alphavantage.co/query

# Finnhub Configuration (get key from https://finnhub.io/)
finnhub.api.key=d95r369r01qgd5f4cgigd95r369r01qgd5f4cgj0
finnhub.api.url=https://finnhub.io/api/v1

# Application Name
spring.application.name=paper-trading-backend
